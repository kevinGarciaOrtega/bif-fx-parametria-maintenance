// Entry point for serverless-offline / Lambda RIC
// Re-export named handler so the handler string uses a single dot:
//   src/handlers/spreadCliente.handler
export { handler } from "./spreadCliente.handler";
